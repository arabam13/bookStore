# Launch this command on terminal, if you want to work on development mode

```bash
export NODE_ENV=development
```

# Excute the commands bellow to install and start server

```bash
cd server
npm install
npm start
```

# Excute the commands bellow to install and start server

```bash
cd client
npm install
npm start
```
